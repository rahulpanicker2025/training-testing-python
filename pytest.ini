[pytest]
# Adds src/ to sys.path so that "from calculator import Calculator" works in tests
# without installing the package. The alternative — pip install -e . — requires a
# pyproject.toml describing the package metadata, which is unnecessary overhead for
# a teaching repo with a single module.
pythonpath = src
